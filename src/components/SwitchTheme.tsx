import { Switch } from '@chakra-ui/react';

const SwitchTheme = () => {
    return (
        <Switch
            colorScheme='pink'>
            Dark Mode
        </Switch>
    );
}

export default SwitchTheme;
